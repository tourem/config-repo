server:
  port: 8765

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

zuul:
  routes:
    products-service: /api/products-service/**
    pricing-service: /api/pricing-service/**
    details-service: /api/details-service/**